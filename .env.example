PORT=8088
DATABASE_URL=postgres://boilerworks:boilerworks@localhost:5447/boilerworks?sslmode=disable
REDIS_URL=redis://localhost:6390/0
SESSION_KEY=change-me-in-production-32-chars!
ENVIRONMENT=development
FRONTEND_URL=http://localhost:3004

# Frontend (Next.js) env vars — set in the frontend environment, not the Go API's.
# NEXT_PUBLIC_API_URL: browser API base (frontend/lib/api.ts); empty = same-origin via Next.js rewrites
# API_URL: server-side rewrite target (frontend/next.config.ts); defaults to http://localhost:8088
#NEXT_PUBLIC_API_URL=
#API_URL=http://localhost:8088
